# P2P — Release Notes 8.4.1

> **Дата:** 2026-07-06 · **Тип:** minor (витрина `8.4.1` + функциональные фиксы) · **Основа:** 8.4.0
> **Внутренние теги сборок НЕ менялись** (`v8C.3 / v8H.3 / v8N.3 / v8L.3`) — маркетинговая версия `8.4.1`
> проставлена в `displayName`/`version` плагинов и заголовках. Имена плагинов (`p2p-v8c3`, `p2p-v8l3`)
> сохранены → **Update у пользователей работает**, команды группируются под `/p2p`.

Линейка: **C** (Claude-native, плагин) · **H** (High) · **N** (Normal/Universal) · **L** (Light/Lite-Live).

---

## 🔑 Главное в 8.4.1

1. **Normal: меню теперь честно отражает загруженные модули.** Опциональные техники [26-31]
   (RAG / Reasoning / Routing / Compression / Security / Optimization) показываются рабочими ТОЛЬКО когда
   их файл-модуль реально приложен; иначе — в 🔒-футере. Экономия токенов = приложи подмножество файлов.
2. **Нативный автодетект хоста в H и N.** Прелоадер сам определяет модель (`SELF_IDENTIFY`, 10 хостов);
   если не уверен — нумерованный выбор `[1..10]` (частый кейс — Qwen). Хост определяется ПЕРЕД меню.
3. **Claude-native: `/p2p` привязан к диспетчеру.** Команда/скилл теперь авто-оркеструют задачу
   (нет задачи → меню; есть задача → SIR → Tier → QUORUM на нативных sub-агентах / co-pilot).
4. **High: поддержка доменов** — новый `!domain.md` (паттерны React / Kotlin).

---

## По сборкам

### 🟦 C — Claude-native (плагин `p2p-v8c3`, маркетплейс)
- **`/p2p` ↔ диспетчер:** `commands/p2p.md` и `skills/p2p/SKILL.md` дополнены поведением ДИСПЕТЧЕРА —
  поднимают бандл `core.md` (PILOT_MODE, TIER_SYSTEM+LoadScore, SIR, DEEP_THINK_VALUE_GATE, QUORUM).
  Нет задачи/`старт`/`menu` → меню; есть задача → авто-роутинг: `Tier ≥ 3` → **QUORUM** (реальные
  параллельные sub-агенты `.claude/agents/*` в Claude Code; в Cowork — скилл `p2p-quorum`),
  `Tier ≤ 2` → co-pilot. _(Закрывает прежний разрыв «плагинная /p2p не авто-роутила».)_
- Витрина: `displayName "P2P 8.4.1-C — Claude Native"`, `version 8.4.1`. Бандл `.plugin` пересобран.

### 🟥 H — High (prompt-файлы)
- **Нативный автодетект хоста** (`files/_preloader.md`, БЛОК 0 `HOST_MODEL_AUTODETECT` + `HOST_PICK_LIST
  [1..10]`, порт механики из 8L.3): `HOST_MODEL` по умолчанию пустой, 10 хостов (+minimax +manus).
- **Новый модуль `files/!domain.md`** — доменные паттерны (React / Kotlin).
- Сопутствующие актуализации: `!!core_v8H`, `!host_profiles`, `!llm_router`, `!routing`, `_index_v8H`,
  `_master_v8H`, `MANIFEST`, `docs/HOST_GUIDE.md`, `README`.

### 🟩 N — Normal / Universal (prompt-файлы)
- **Статичный меню-гейт в ядре** (`!!core_v8N.md §4`): `EXTENSIONS_SCAN` + `AVAILABILITY` +
  `MENU_RENDER_ALGORITHM`. Пункт [26-31] AVAILABLE ⇔ тело его файла-модуля в контексте (детект по
  заголовку «… MODULE (!x.md)» + frontmatter `id`/`menu_item`; упоминания в base НЕ считаются).
  Флаг `MODULE_*=true` без файла → пункт остаётся 🔒 (не галлюцинируем содержимое).
- **Фикс бага нумерации:** устранён дублировавшийся пункт «26» (`/p2p-download` → в слэш-команды);
  модули заняли чистые [26-31] с тегами `[MODULE: !x.md]`.
- **Хост:** `HOST_MODEL_AUTODETECT` (10 хостов, `HOST_MODEL=""`) + `HOST_PICK_LIST [1..10]` (фикс Qwen);
  хост определяется/выбирается ПЕРЕД меню. Якоря: `_index.md EXTENSIONS_ANCHOR`.
- **Архитектурное решение:** эксперимент base/extended-сплита **отброшен** (переусложнение) — Normal
  остался ПЛОСКИМ (все модули в `files/`), «показывать не все» решает меню-гейт.
- Docs: `HOST_GUIDE` (автодетект/выбор), `FAQ_И_ОШИБКИ` (про 🔒-модули и Qwen), `CHANGELOG_v8N3`.

### 🟨 L — Light / Lite-Live Hybrid (плагин `p2p-v8l3`, **в архиве релиза**)
- Витрина: `displayName "P2P 8.4.1-L — Lite/Live Hybrid (Universal)"`, `version 8.4.1`; бандл `.plugin`.
- Механика без изменений: FETCH-канарейка + GIST-lazy (см. `docs/` L), 8 хостов.

---

## Установка / маркетплейс

- **Маркетплейс `P2P-4PDA-edition` публикует ОДИН плагин** — `p2p-v8c3` (Claude-native). Это осознанно и
  соответствует рекомендации Anthropic (один маркетплейс = один плагин), как и в 8.4.0.
- **Light (`p2p-v8l3`) поставляется ВНУТРИ архива релиза** для ручной установки — отдельно на странице
  релиза не публикуется, чтобы пользователи не путались.
- **H / N** — не плагины, а наборы prompt-файлов (заливаются в чат/Projects; заливать можно подмножество).

---

## Совместимость / откат
- Обратная совместимость полная; внутренние теги `v8X.3` не менялись → Update не ломается.
- Бэкап прежних `editions/` перед сборкой 8.4.1 → `_BACKUPS/2026-07-06_pre-8.4.1/editions/`.
- Публикация (ветка + PR + push, без force-push; email только `sanic732@protonmail.com`) — через
  skill `p2p-release`, выполняется отдельно.
