# Release artifacts (built locally, never committed)
*.plugin
*.zip

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE / editor
.idea/
.vscode/
*.swp
*~

# Local node/python (defensive — repo is markdown-only)
node_modules/
__pycache__/
*.pyc

# Local Claude Code overrides (anywhere in the tree)
**/settings.local.json
.claude/settings.local.json

# Staging / artifacts dirs (in case someone clones into a parent)
_release-staging/
_release-artifacts/
