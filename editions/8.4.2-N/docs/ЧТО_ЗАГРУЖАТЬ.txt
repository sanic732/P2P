================================================================================
  P2P 8.4.2-N (Normal / Universal) — КАКИЕ ФАЙЛЫ ГРУЗИТЬ И СКОЛЬКО ЭТО ТОКЕНОВ
  Обновлено: 2026-07-07. Токены посчитаны токенайзером (gpt-tokenizer) по реальным
  файлам этой сборки; у конкретной модели счёт может отличаться примерно на +/-10%.
================================================================================

КУДА ГРУЗИТЬ: в контекст любого из 8 хостов (claude / gemini / gpt / grok /
deepseek / qwen / kimi / glm) — вложениями, в Project Knowledge или system prompt.
После загрузки написать: старт

--------------------------------------------------------------------------------
РАЗДЕЛ 1. ОБЯЗАТЕЛЬНЫЙ МИНИМУМ — 6 файлов, ~27 000 токенов
--------------------------------------------------------------------------------
Грузить строго в этом порядке (это REQUIRED-порядок из _index.md).

  1. _preloader.md            ~3 500   определение хоста, настройки, порядок загрузки
  2. !!core_v8N.md            ~8 700   ядро: диспетчер, меню, протоколы, роутинг
  3. !!db_v8N.md              ~6 100   база: техники, ошибки G1-G20, веса QUORUM
  4. _live/MANIFEST.md        ~1 800   манифест версий, дедлайны моделей
  5. _live/live_core.md       ~2 700   цены, Arena ELO, матрица роутинга
  6. _live/live_vendors.md    ~4 600   каталог G-ошибок, правила трансляции

  ИТОГО МИНИМУМ:             ~27 000 токенов

--------------------------------------------------------------------------------
РАЗДЕЛ 2. ОСТАЛЬНЫЕ ФАЙЛЫ — грузить ПО ЗАДАЧЕ (все опциональны)
--------------------------------------------------------------------------------
Модули ON-DEMAND (добавляешь к минимуму, когда нужна функция):

  1. !agents.md               ~3 900   QUORUM: 8 агентов, sub-паттерны (симуляция)
  2. !pipeline.md             ~3 500   Contract Builder, шаблоны, 5D-интент
  3. !toolkit.md              ~3 800   debug, Arena, writing, enhance, combinator
  4. !scope.md                ~1 600   SCOPE.HELM: большие многошаговые задачи
  5. !memory.md               ~1 600   протокол CAPSULE (сохранение состояния)
  6. !metrics.md              ~1 300   метрики сессии, Routing Memory
  7. !sandbox.md              ~1 000   режим исследования / песочница

Модули v8N.3 (пункты меню [26]-[31]; включаются и через MODULE_*-флаги):

  8. !rag.md                  ~1 300   RAG / RAPTOR / векторный поиск       [26]
  9. !reasoning.md            ~1 400   CoT / Self-Consistency / MCTS        [27]
 10. !routing.md              ~1 700   советник по выбору модели            [28]
 11. !compression.md          ~1 300   сжатие контекста: LLMLingua / Gist   [29]
 12. !security.md             ~1 300   аудит промптов, защита от инъекций   [30]
 13. !optimization.md         ~1 300   автооптимизация промптов APO/OPRO    [31]

Справочники по вендорам (когда генерируешь промпт под конкретную модель):

 14. vendors/tier1.md         ~2 200   топ: Fable 5, Opus 4.8/4.7, GPT-5.5, Gemini 3.1 Pro
 15. vendors/tier2.md         ~1 500   сильные: Sonnet 4.6, Grok 4.3, DeepSeek V4-Pro
 16. vendors/tier3.md           ~800   быстрые: Gemini Flash, DeepSeek V4-Flash, Haiku 4.5
 17. vendors/tier4.md           ~900   открытые: GLM-5.1 (MIT), Kimi K2.x (Swarm)

Служебные и справочные (обычно НЕ грузятся в контекст):

 18. _live/live_specs.md     ~27 800  свежий срез цен/квот/багов (OVERRIDE поверх
                                      live_core). ВНИМАНИЕ: весит как весь минимум;
                                      грузить только если нужны актуальные цены
 19. _index.md                ~2 300  реестр всех файлов (навигация для человека)
 20. _master.md               ~1 800  гайд сборки для API
 21. docs/*                   1-3 K   документация для чтения человеком, не для LLM

--------------------------------------------------------------------------------
ТИПОВЫЕ НАБОРЫ (минимум + модули)
--------------------------------------------------------------------------------
  Просто генерировать промпты:   минимум + !pipeline                    ~31 000
  Мульти-агентный разбор:        минимум + !agents                      ~31 000
  Большая многошаговая задача:   минимум + !scope + !memory             ~30 000
  Всё сразу (не рекомендуется):  все файлы разделов 1-2                 ~65 000

--------------------------------------------------------------------------------
ПРИМЕЧАНИЯ
--------------------------------------------------------------------------------
1. Не смешивать файлы разных редакций (C/H/N/L) — ядра несовместимы.
2. Порядок минимума важен: _preloader всегда первый, ядро до модулей.
3. Модули !*.md можно догружать в уже идущую сессию — система их подхватит.
4. На старте система спросит хост-модель — это нормально, выбери свою.
================================================================================
