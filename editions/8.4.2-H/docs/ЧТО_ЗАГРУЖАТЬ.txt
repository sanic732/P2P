================================================================================
  P2P 8.4.2-H (High \ Hybrid) — КАКИЕ ФАЙЛЫ ГРУЗИТЬ И СКОЛЬКО ЭТО ТОКЕНОВ
  Обновлено: 2026-07-07. Токены посчитаны токенайзером (gpt-tokenizer) по реальным
  файлам этой сборки; у конкретной модели счёт может отличаться примерно на +/-10%.
================================================================================

КУДА ГРУЗИТЬ: в контекст любого из 8 хостов (grok — нативный Heavy-16, а также
claude / gemini / gpt / deepseek / qwen / kimi / glm) — вложениями, в Project
Knowledge или system prompt. После загрузки написать: старт

--------------------------------------------------------------------------------
РАЗДЕЛ 1. ОБЯЗАТЕЛЬНЫЙ МИНИМУМ — 8 файлов, ~32 000 токенов
--------------------------------------------------------------------------------
Грузить строго в этом порядке. У редакции H минимум больше, чем у N: два
host-engine файла обязательны всегда (на них держится выбор хоста и роутинг).

  1. _preloader.md            ~4 400   определение хоста, настройки, порядок загрузки
  2. !!core_v8H.md            ~9 000   ядро: диспетчер, меню, протоколы, роутинг
  3. !!db_v8H.md              ~6 600   база: техники, ошибки G1-G20, веса QUORUM
  4. _live/MANIFEST.md        ~1 700   манифест версий, дедлайны моделей
  5. _live/live_core.md       ~2 700   цены, Arena ELO, матрица роутинга
  6. _live/live_vendors.md    ~4 600   каталог G-ошибок, правила трансляции
  7. !host_profiles.md        ~1 400   профили 8 хостов (HOST_CAPS, gate Heavy-16)
  8. !llm_router.md           ~1 900   мульти-провайдерный роутер моделей

  ИТОГО МИНИМУМ:             ~32 000 токенов

  ЕСЛИ ХОСТ — GROK, добавь девятым (обязателен именно на Grok):

  9. !tool_budget.md            ~500   контроль бюджета инструментов Heavy-16

  ИТОГО МИНИМУМ НА GROK:     ~33 000 токенов

--------------------------------------------------------------------------------
РАЗДЕЛ 2. ОСТАЛЬНЫЕ ФАЙЛЫ — грузить ПО ЗАДАЧЕ (все опциональны)
--------------------------------------------------------------------------------
Модули ON-DEMAND (добавляешь к минимуму, когда нужна функция):

  1. !agents.md               ~1 800   QUORUM: 8 агентов, sub-паттерны
  2. !pipeline.md             ~3 500   Contract Builder, шаблоны, 5D-интент
  3. !toolkit.md              ~3 800   debug, Arena, writing, enhance, combinator
  4. !scope.md                ~1 600   SCOPE.HELM: большие многошаговые задачи
  5. !memory.md               ~1 600   протокол CAPSULE (сохранение состояния)
  6. !metrics.md              ~1 300   метрики сессии, Routing Memory
  7. !sandbox.md              ~1 000   режим исследования / песочница
  8. !domain.md               ~4 500   домен-знания: React 19 / Kotlin / KMP  [41]

Host-engine по запросу:

  9. !routing_matrix.md         ~800   аудируемая карта маршрутизации v2.0
 10. !x_realtime.md             ~500   X Firehose (ТОЛЬКО Grok; платный gate $0.50)

Модули v8H.3 (пункты меню [35]-[40]; включаются и через MODULE_*-флаги):

 11. !rag.md                  ~1 300   RAG / RAPTOR / векторный поиск       [35]
 12. !reasoning.md            ~1 400   CoT / Self-Consistency / MCTS        [36]
 13. !routing.md              ~1 900   советник по выбору модели            [37]
 14. !compression.md          ~1 300   сжатие контекста: LLMLingua / Gist   [38]
 15. !security.md             ~1 300   аудит промптов, защита от инъекций   [39]
 16. !optimization.md         ~1 300   автооптимизация промптов APO/OPRO    [40]

Справочники по вендорам (когда генерируешь промпт под конкретную модель):

 17. vendors/grok.md            ~500   Grok 4.3 / Heavy-16 / X Firehose
 18. vendors/claude.md          ~600   Fable 5, Opus 4.8/4.7, Sonnet 4.6
 19. vendors/tier1.md         ~2 200   топ: Fable 5, Opus 4.8/4.7, GPT-5.5, Gemini 3.1 Pro
 20. vendors/tier2.md         ~1 500   сильные: Sonnet 4.6, Grok 4.3, DeepSeek V4-Pro
 21. vendors/tier3.md           ~800   быстрые: Gemini Flash, DeepSeek V4-Flash, Haiku 4.5
 22. vendors/tier4.md           ~900   открытые: GLM-5.1 (MIT), Kimi K2.x (Swarm)

Служебные и справочные (обычно НЕ грузятся в контекст):

 23. _live/live_specs.md     ~27 800  свежий срез цен/квот/багов (OVERRIDE поверх
                                      live_core). ВНИМАНИЕ: весит почти как весь
                                      минимум; грузить только ради актуальных цен
 24. _index_v8H.md            ~2 600  реестр всех файлов (навигация для человека)
 25. _master_v8H.md           ~1 800  гайд сборки для API
 26. docs/*                   1-3 K   документация для чтения человеком, не для LLM

--------------------------------------------------------------------------------
ТИПОВЫЕ НАБОРЫ (минимум + модули)
--------------------------------------------------------------------------------
  Просто генерировать промпты:   минимум + !pipeline                    ~36 000
  Heavy-16 на Grok:              минимум-grok + !agents + vendors/grok  ~35 000
  Мульти-агентный разбор:        минимум + !agents                      ~34 000
  Большая многошаговая задача:   минимум + !scope + !memory             ~35 000
  Всё сразу (не рекомендуется):  все файлы разделов 1-2                 ~75 000

--------------------------------------------------------------------------------
ПРИМЕЧАНИЯ
--------------------------------------------------------------------------------
1. Не смешивать файлы разных редакций (C/H/N/L) — ядра несовместимы.
2. Порядок минимума важен: _preloader всегда первый, ядро до модулей.
3. Модули !*.md можно догружать в уже идущую сессию — система их подхватит.
4. На старте система спросит хост-модель — выбери свою; профиль хоста
   (!host_profiles.md) определит, что доступно нативно (Heavy-16 только Grok).
================================================================================
