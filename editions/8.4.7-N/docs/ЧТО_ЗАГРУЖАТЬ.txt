================================================================================
  P2P 8.4.3-N (Normal / Universal) — КАКИЕ ФАЙЛЫ ГРУЗИТЬ И СКОЛЬКО ЭТО ТОКЕНОВ
  Обновлено: 2026-07-14. Токены пересчитаны токенайзером (o200k / gpt-tokenizer)
  по РЕАЛЬНЫМ файлам этой сборки; у конкретной модели счёт может отличаться на +/-10%.
================================================================================

КУДА ГРУЗИТЬ: в контекст любого из 8 хостов (claude / gemini / gpt / grok /
deepseek / qwen / kimi / glm) — вложениями, в Project Knowledge или system prompt.
После загрузки написать: старт

--------------------------------------------------------------------------------
РАЗДЕЛ 1. ОБЯЗАТЕЛЬНЫЙ МИНИМУМ — 6 файлов, ~29 000 токенов
--------------------------------------------------------------------------------
Грузить строго в этом порядке (это REQUIRED-порядок из _index.md).

  1. _preloader.md            ~4 000   определение хоста, настройки, порядок загрузки
  2. !!core_v8N.md            ~8 700   ядро: диспетчер, меню, протоколы, роутинг
  3. !!db_v8N.md              ~6 300   база: техники, ошибки G1-G20, веса QUORUM
  4. _live/MANIFEST.md        ~1 800   манифест версий, дедлайны моделей
  5. _live/live_core.md       ~3 100   цены, Arena ELO, матрица роутинга
  6. _live/live_vendors.md    ~5 300   каталог G-ошибок, правила трансляции

  ИТОГО МИНИМУМ:             ~29 200 токенов

--------------------------------------------------------------------------------
РАЗДЕЛ 2. ОСТАЛЬНЫЕ ФАЙЛЫ — грузить ПО ЗАДАЧЕ (все опциональны)
--------------------------------------------------------------------------------
Модули ON-DEMAND (добавляешь к минимуму, когда нужна функция):

  1. !agents.md               ~3 900   QUORUM: 8 агентов, sub-паттерны (симуляция)
  2. !pipeline.md             ~3 900   Contract Builder, шаблоны, 5D-интент
  3. !toolkit.md              ~3 700   debug, Arena, writing, enhance, combinator
  4. !scope.md                ~1 600   SCOPE.HELM: большие многошаговые задачи
  5. !memory.md               ~1 600   протокол CAPSULE (сохранение состояния)
  6. !metrics.md              ~1 300   метрики сессии, Routing Memory
  7. !sandbox.md              ~1 000   режим исследования / песочница

ON-DEMAND модули (пункты меню [26]-[32]; включаются и через MODULE_*-флаги):

  8. !rag.md                  ~1 300   RAG / RAPTOR / векторный поиск       [26]
  9. !reasoning.md            ~1 400   CoT / Self-Consistency / MCTS        [27]
 10. !routing.md              ~1 700   советник по выбору модели            [28]
 11. !compression.md          ~1 300   сжатие контекста: LLMLingua / Gist   [29]
 12. !security.md             ~1 300   аудит промптов, защита от инъекций   [30]
 13. !optimization.md         ~1 300   автооптимизация промптов APO/OPRO    [31]
 14. !skills.md               ~1 900   генератор Agent Skills (SKILL.md)    [32]

Справочники по вендорам (когда генеришь промпт под конкретную модель):

 15. vendors/tier1.md         ~2 700   топ: Fable 5, Sonnet 5, Opus 4.8/4.7, GPT-5.6, Gemini 3.1 Pro
 16. vendors/tier2.md         ~1 700   сильные: Grok 4.5/4.3, DeepSeek V4-Pro, Qwen
 17. vendors/tier3.md           ~800   быстрые: Gemini 3.5 Flash, DeepSeek V4-Flash, Haiku 4.5
 18. vendors/tier4.md         ~1 200   открытые: GLM-5.2 (MIT), Kimi K2.6/K2.7 (Swarm)

Служебные и справочные (обычно НЕ грузятся в контекст):

 19. _live/live_specs.md     ~10 600  свежий срез цен/квот/багов (OVERRIDE поверх
                                      live_core). С 2026-07-14 — только ДЕЛЬТА
                                      (стабильные спеки моделей уже в tier/live_vendors),
                                      было ~27 800 → стало ~10 600
 20. _index.md                ~2 400  реестр всех файлов (навигация для человека)
 21. _master.md               ~1 800  гайд сборки для API
 22. docs/*                   1-3 K   документация для чтения человеком, не для LLM

--------------------------------------------------------------------------------
ТИПОВЫЕ НАБОРЫ (минимум + модули)
--------------------------------------------------------------------------------
  Просто генерировать промпты:   минимум + !pipeline                    ~33 100
  Мульти-агентный разбор:        минимум + !agents                      ~33 100
  Большая многошаговая задача:   минимум + !scope + !memory             ~32 400
  Всё сразу (не рекомендуется):  все файлы разделов 1-2                 ~62 000

--------------------------------------------------------------------------------
ПРИМЕЧАНИЯ
--------------------------------------------------------------------------------
1. Не смешивать файлы разных редакций (C/H/N/L) — ядра несовместимы.
2. Порядок минимума важен: _preloader всегда первый, ядро до модулей.
3. Модули !*.md можно догружать в уже идущую сессию — система их подхватит.
4. На старте система спросит хост-модель — это нормально, выбери свою.
5. live_specs с 2026-07-14 — тонкая дельта: полные спеки моделей (цены, контекст,
   тиры) живут в vendors/tier*.md и _live/live_vendors.md, а live_specs перебивает
   их только по свежим изменениям (правило OVERRIDE при более новом VERSION).
================================================================================
